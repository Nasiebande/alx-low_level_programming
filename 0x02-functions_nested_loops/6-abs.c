#include "main.h"

/**
 * _abc - Computes the absolute value of an integer
 * @t: The integer to be computed
 * Return: The absolute value
 */
imt _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
